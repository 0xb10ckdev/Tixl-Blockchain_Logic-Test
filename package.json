{
  "name": "tixl-coding-challenge",
  "version": "1.0.0",
  "description": "Tixl coding challenge",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "tsc && node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "better-sqlite3": "^7.1.1",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "sha256": "^0.2.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^5.4.1",
    "@types/express": "^4.17.9",
    "@types/node": "^14.14.11",
    "@types/sha256": "^0.2.0",
    "tslint": "^6.1.3",
    "typescript": "^4.1.2"
  }
}
